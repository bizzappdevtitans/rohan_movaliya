<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!-- server action sor sending mail when sechduled training session #T00468 -->
    <record id="model_send_taining_mail" model="ir.actions.server">
        <field name="name">Send Mail</field>
        <field name="model_id" ref="model_student_record" />
        <field name="binding_model_id" ref="model_student_record" />
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = records.action_send_email()</field>
    </record>
</odoo>
