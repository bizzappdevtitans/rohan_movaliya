<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <data>
        <!-- server action for sending mail when scheduled training session #T00468 -->
        <record id="model_send_taining_mail" model="ir.actions.server">
            <field name="name">Training</field>
            <field name="model_id" ref="model_student_student" />
            <field name="binding_model_id" ref="model_student_student" />
            <field name="binding_view_types">list,form</field>
            <field name="state">code</field>
            <field name="code">
                for record in records:
                    record.action_send_email_for_training() </field>
        </record>

        <!-- server action for sending mail when company come for placement #T00468 -->
        <record id="model_send_company_interview_mail" model="ir.actions.server">
            <field name="name">Interview</field>
            <field name="model_id" ref="model_student_student" />
            <field name="binding_model_id" ref="model_student_student" />
            <field name="binding_view_types">list,form</field>
            <field name="state">code</field>
            <field name="code">
                for record in records:
                    record.action_send_email_for_placement()</field>
        </record>
    </data>
</odoo>
